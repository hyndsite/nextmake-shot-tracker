export default function Heatmap({ mode }){
    return (
      <div className="page">
        <h1 className="h1">Heatmap ({mode})</h1>
        <div className="card">Interactive court heatmap (FG% / Density) with filters.</div>
      </div>
    )
  }
  